// This will be the pairing button. Click on the button to pair, then when paired, render the 'connet' view

import React, {
  Component,
  Text,
  View,
} from 'react-native';

import MK, {
  MKButton,
} from 'react-native-material-kit';

